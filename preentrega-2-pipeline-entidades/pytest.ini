[pytest]
# Agrega la raíz del proyecto a sys.path para que los tests puedan hacer
# "from chain import ..." / "from schemas import ..." sin importar desde
# qué directorio se ejecute pytest.
pythonpath = .
